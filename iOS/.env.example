# Mixpanel Configuration
# Copy this file to .env and add your actual tokens
# DO NOT commit .env to version control

# Debug Environment (Development builds)
MIXPANEL_DEBUG_TOKEN=your_debug_project_token_here

# Production Environment (Release builds)
MIXPANEL_PROD_TOKEN=your_production_project_token_here

# Instructions:
# 1. Get your tokens from https://mixpanel.com → Project Settings
# 2. Copy this file: cp .env.example .env
# 3. Replace the placeholder values with your actual tokens
# 4. The .env file is gitignored and won't be committed